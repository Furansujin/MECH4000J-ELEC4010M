%% Project 2
clear all, clc;

%% Define virtices for the polygon
v = [600, 0; 800, 200; 700, 300; 500, 310;];

u = 1.5;
ang = rad2deg(atan(u));
polygon(v);
hold on
[x0, y0, x1, y1, fc] = det_fr_cls(v, 2, 3, 9, 2, ang);
plt_rbt(v, x0, y0, x1, y1, fc);